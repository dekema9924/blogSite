

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <title>MyBlog</title>
</head>
<body>
 
    <% blogs.forEach(blog=>{%>    
           <div class="container mt-3">
                <h1><%= blog.title %></h1>
                <p><%= blog.date.toDateString() %></p> 


                <a href="/routes"  class="btn btn-primary" >All articles</a>
                <a href="/routes/edit/<%= blog.id %>" class="btn btn-secondary">Edit</a>
           </div>   
   <% }) %>

 

    <hr>

    <% blogs.forEach(blog=>{%>  
        <div class="container">
            <p><%= blog.markdown %></p>
        </div>    
      
    <% }) %>
   
</body>