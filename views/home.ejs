
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="style.css">
    <title>MyBlog</title>
</head>
<body>
 <div class="container ml-5">
    <h1>Blog Articles</h1>
    <a href="/routes/new" class="btn btn-success">New article</a>


    <% articles.forEach(article => {%>
        <div class="container-sm mt-3 ">
           <p class="h1"><%=article.title%></p>
           <p class="text-info"><%= article.submission_date.toDateString()%></p>
           <p class="text-secondary"><%= article.description%></p>
           <div class="container mb-3">
                <a href="/routes/readmore/<%= article.id%>" class="btn btn-primary">ReadMore</a>
                <a href="/routes/edit/<%= article.id%>" class="btn btn-info">Edit</a>

                <form action="/routes/<%= article.id%>?_method=DELETE" method="post" class="d-inline">
                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>

           </div>

        </div>
       
    <% }); %>

 </div>
    
</body>
</html>




